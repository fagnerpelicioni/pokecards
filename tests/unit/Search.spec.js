/* eslint-disable */

import Search from '@/components/pokemons/Search.vue';

describe('Search', () => {
  test('If has found a Pokemon, shows the card', () => {
    expect(true).toBe(true);
  });

  test('If has no found a Pokemon, shows error message', () => {
    expect(true).toBe(true);
  });
});
